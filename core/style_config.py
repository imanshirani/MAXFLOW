# core/style_config.py

class NodeColors:
    # ==========================================
    # Header Colors
    # ==========================================
    HEADER_SCENE = (40, 100, 120)    # Dark Turquoise for scenes
    HEADER_MODIFIER = (110, 60, 120) # Dark Violet for modifiers
    HEADER_MATH = (50, 90, 60)       # Forest Green for math
    HEADER_INPUT = (150, 60, 60)     # Brick Red for inputs
    HEADER_EXTRACT = (180, 110, 40)  # Orange for extract

    # ==========================================
    # Port Colors (Data Types)
    # ==========================================
    PORT_OBJECT = (0, 200, 150)      # Turquoise for objects
    PORT_VECTOR = (150, 100, 210)    # violet for vectors
    PORT_FLOAT = (160, 160, 160)     # gray for floats
    PORT_INT = (80, 200, 120)        # Phosphorous Green for integers
    PORT_BOOL = (200, 100, 150)      # Pink for Boolean (turn on/off)
    PORT_STRING = (100, 150, 200)    # Sky Blue for String